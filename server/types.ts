export type User = {
  id: string,
  username: string,
  email: string,
  password: string
}

export type Password = {
  id: string,
  title: string,
  description: string,
  password: string,
  createdAt: string
}
